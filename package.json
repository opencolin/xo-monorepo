{
  "name": "xo-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "XO — unified monorepo for the XO agent-workspace platform: web apps, Python services, agent kits, automations and content.",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
