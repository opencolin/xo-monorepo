/**
 * @deprecated Removed from the landing page in May 2026.
 *
 * This section showed host environments orbiting a workspace tile:
 * Laptop, Coder, Docker, Vercel Sandbox, Mac mini, self-host. Most of
 * those are roadmap deployment targets (per xo-room/product.mdx). The
 * landing page now ships only live features, so this section was
 * replaced by `<ValueProps />`.
 *
 * Safe to delete this file. Nothing imports it. It's only kept here
 * because the dev environment's filesystem is read-mostly. Drop it
 * when you next clean up the repo.
 */
export {};
